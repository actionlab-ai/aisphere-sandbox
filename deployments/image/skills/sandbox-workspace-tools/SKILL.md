# Sandbox Workspace Tools

Use workspace list before reading unknown paths, search text for keywords, read before editing, and write for creating or replacing files. All paths are relative to /workspace. Do not use host paths.
